# 2026 draft strategy conclusions (Phase 3 exit deliverable) -- 2026-07-10

**Audience:** the league manager, setting 2026 draft policy.
**How to read this doc:** the numbered sections below are HAND-WRITTEN judgment,
grounded only in the evidence tables at the bottom (regenerated by
`scripts/strategy_conclusions.py`) plus the cited Phase 2 / Task 3 research docs.
The GENERATED EVIDENCE block is machine-produced and carries its own data
vintage; re-running the script refreshes it in place without touching this prose.
**All hand-written figures above (percentages, ranks, correlations) are
vintage-locked to the generated block dated 2026-07-10 below; if the script is
re-run and the block's numbers change, this prose must be re-checked against
the new numbers before being trusted.**

**The one integrity rule this whole doc obeys:** the sim farm scores seasons in
a Monte-Carlo evaluator that OVER-rewards our own seat (farm all-play% clusters
0.64-0.73; the backtest against ACTUAL 2023-25 points sits at ~0.53). So no
absolute farm win rate below is an expectation. Only **cross-cell deltas** (which
plan beats which) are load-bearing, and `top3_rate` (saturated ~0.994) is not
cited at all.

---

## Headline recommendations (one per line)

1. **QB: build a 2-QB core; do NOT front-load a 3-QB hoard early.** Take QB1 when VORP dictates (typically R1-2 in this superflex/2-QB league), but do not reach to stack 2-3 QBs in the first handful of rounds. Confidence: moderate-high on "don't front-load," lower on exactly how much to delay.
2. **DEF/K: draft them in the last ~5-6 rounds (round 14-18) or stream -- not early.** This REVISES Phase 2's "draft early" verdict at the level of *action*. Confidence: high that round 8-11 is wrong; moderate on 14 vs 18.
3. **Tier-break bonus: drop it (keep `tier_break_bonus=0.0`).** No measurable benefit; the effect is -0.1% (within noise). Confidence: high.
4. **Sim-vs-reality transfer of QB fine-ordering is weak (Spearman 0.20).** Trust only the coarse agreement (front-loading QB is worst) and rank the backtest above the sim for QB-timing fine-tuning.

---

## 1. QB-timing policy

**Recommendation: secure a 2-QB core, take QB1 roughly when VORP says (often
R1-2 here), and do NOT reach to hoard 2-3 QBs early. Let the 3rd QB come late,
or not at all.**

The evidence splits into a robust part and a fragile part, and the honest policy
uses only the robust part.

**Robust (both methods agree): front-loading QB is the worst plan.** qb_plan 0
(`qb_not_before=(1,1,1)` -- take QBs as raw VORP dictates, which front-loads them
because the top ~25 VORP in a 2-QB league are all QBs) is ranked **worst of the
six** by BOTH the farm (MC-inflated) AND the backtest (real 2023-25 points) -- see
the R7 table. That is the single cross-method agreement, and it is the one QB
conclusion that survives every caveat.

**Fragile (methods disagree -- do NOT over-read the farm): how far to delay.**
The farm, read alone, prefers the *most aggressive* delay (qb_plan 3, QB1 not
before R3) and ranks it best. **The backtest does not corroborate this** -- it
ranks that same aggressive-delay plan near the bottom (2nd worst) and instead
favors a 2-QB build (qb_plan 5) and a light stagger (qb_plan 1). Because the
backtest scores on real points and the farm's absolute levels are known-inflated,
**we rank the backtest higher here**: the farm's reward for aggressive delay is
most likely an MC-evaluator artifact, not real edge.

### Caveat (BINDING -- must travel with this conclusion)

The sim's opponents take their first QB at a mean round of **2.84 vs the league's
historical 1.83** (assumption-audit WARN, `reports/sim-farm-2026-07-10.md`). The
sim's opponents therefore leave QBs on the board LONGER than the real league does,
which makes any delayed-QB strategy look *safer in simulation than it will be in
your actual draft* -- if your real opponents grab QBs a full round earlier, the QBs
you were planning to wait on may be gone. This cuts directly against pushing the
delay far. It is also why the backtest's refusal to reward aggressive delay
(qb_plan 3) is worth weighting: even under the same QB-friendly opponent model,
real-points scoring did not pay for waiting. Net: delay the *hoard*, not
necessarily QB1.

_This also resolves the Phase 2 finding (`2026-07-09-baseline-sensitivity.md`)
that the QB-hoard scenario reorders the top-24 board 10/24 between hoard_0 and
hoard_12/24: the qb_subgrid table shows the plan ORDERING is stable across all
three hoard scenarios -- the two earliest-QB plans (0 and 4) sit at the bottom
in every scenario within noise (e.g. qb_hoard_12: plan 4 at 68.8% vs plan 0 at
69.1%, CI-overlapping), and plans 2/3 are best in each -- so the board churn
does not change the timing policy -- it changes which QBs, not when._

## 2. DEF/K policy (Phase 2 verdict revised in decision context)

**Recommendation: draft DEF and K in the last ~5-6 rounds (round 14-18), or
stream them in-season. Do not spend a round-8-11 pick on either.**

There is a genuine tension to adjudicate, and it resolves cleanly once the two
pieces of evidence are scoped to the different questions they actually answer:

- **Phase 2 (`2026-07-09-def-k-streaming-baseline.md`) says DRAFT EARLY.** A
  *hindsight-elite* DEF beat a realistic streamer by **+6.96 pts/wk** (K:
  **+4.07 pts/wk**), both above the 1.5 pts/wk threshold. This says: elite DEF/K
  carry real, material value.
- **The farm says LATER IS BETTER**, monotonically: mean all-play% climbs
  66.3% -> 68.0% -> 70.1% -> 71.1% as `defk_round` moves 8 -> 11 -> 14 -> 18
  (+4.8 points across the range; the bulk of the penalty is at round 8).

These are **not contradictory** -- they measure different things:

1. Phase 2's +6.96/+4.07 is an **upper bound** obtained with *hindsight* (top-3
   season-total, "no one drafts knowing who finishes top-3," per that doc's own
   verdict rule). It is the value of the elite DEF/K *if you could identify it in
   advance.*
2. **The projection tool available to us does not exploit the signal that
   exists.** Year-over-year DEF rank correlation is **ρ=0.572** (r²≈0.33,
   `2026-07-10-dst-semantics.md`) -- that is a moderate, partially usable
   preseason signal, not a negligible one. But Sleeper's season projection does
   not use it: it emits no tier buckets, only a flat league-average uplift
   (9.66 pts/wk added identically to every team). So with the tools this league
   actually has, the "elite" DEF cannot be told apart from the field preseason
   -- not because prior-year rank is uninformative, but because the projection
   feeding the farm ignores it.
3. The farm measures the **opportunity cost** of spending an early pick on the
   best-*projected* DEF/K in a full draft -- i.e. the skill player you forgo. That
   skill-player VORP is the part of the farm the MC evaluator captures *well*
   (it is our seat's core competency), so the "later is better" direction is
   driven by a well-measured quantity, not by DEF projection quality.

**Adjudication:** the prize Phase 2 found is real, but it is not capturable with
current projection precision, so in the full-draft decision the opportunity cost
dominates. The rational action is to draft DEF/K late or stream.

**Confidence:** HIGH that early (round 8-11) is wrong -- the farm penalty is
large, monotonic, AND the mechanism (Sleeper's flat, non-differentiating DEF
projection) is independently documented. MODERATE on 14 vs 18 (the 14->18 delta
is only ~1 point). One honest asterisk: because the farm's DEF projection
signal is flat by construction (constraint #3), it *cannot* credit a genuinely
well-projected elite DEF for
offsetting the pick cost -- so if you hold strong *non-projection* conviction on a
specific defense (schedule, external expert consensus), an earlier pick is not
punished quite as hard as the farm's -4.8 implies. Absent that conviction:
late/stream.

## 3. Tier-break rule

**Recommendation: DROP the tier-break bonus (keep `tier_break_bonus=0.0`).**

Adding an 8-point score bump for closing out a positional tier changed mean
all-play% by **-0.1%** across 24 vs 24 main-grid cells -- i.e. nothing, and if
anything slightly negative. It adds a knob and a rationale ("grab the last player
in a tier before a cliff") that the evidence does not support at this league's
tier structure. Confidence: high. (Note: the farm did not grid the positional
*caps*, so no caps recommendation is made -- that would be fabricated evidence.)

## 4. Sim-vs-backtest agreement (R7's earliest signal)

**Result: Spearman ρ = 0.20 (n=6, p=0.70) between the farm and backtest QB-plan
orderings -- weak-positive, BELOW the 0.4 "material transfer" bar. R7 has partially
fired.**

R7 asked whether the sim farm's strategy ordering transfers to the non-circular
backtest. Task 11 left this uncomputable against the stored ADR-D7 reference,
because those 4 reference strategies differ only in `qb_by_round`, which never
binds under qb_hoard_12 VORP (the real delay knob, `qb_not_before`, did not exist
when the reference ran) -- so they produce identical drafts and a degenerate
ordering. **We took option (a):** re-ran the farm's actual 6 `(qb_not_before,
qb_by_round)` plans through the `ffi.sim.backtest` machinery in memory (3 seasons
× 100 seeded drafts each, scored on ACTUAL nflverse points, `defk_round=18`, DEF
neutralized), computed the real ordering, and correlated it against the farm's
`defk_round=18` slice. **Nothing was persisted; the ADR D7 reference row is
untouched.** The `qb_not_before` knob genuinely binds in the backtest (QB1-round
means move 1 -> 2 -> 3 across plans -- see the R7 table), so the check is real,
not degenerate.

**Interpretation:** the fine-grained ordering does NOT transfer well. The one
agreement is coarse and robust -- both methods rank the front-loaded plan
(qb_plan 0) worst -- but past that they disagree, and notably the backtest
ranks the farm's *favorite* (aggressive-delay qb_plan 3) near the bottom. Per the
brief's rule, a sub-0.4 correlation means **the backtest evidence outranks the sim
evidence** wherever they conflict, which is exactly how Section 1 is written: trust
"don't front-load," discount the farm's push for aggressive delay. (n=6 gives the
p-value no power; the rank agreement, not the p-value, is the signal.)

## 5. Caveats (the honest boundary of all of the above)

- **MC-evaluator inflation.** Every farm all-play% is inflated by the no-bust /
  no-injury-shock Monte-Carlo season model (farm 0.64-0.73 vs backtest ~0.53).
  Only cross-cell deltas are used anywhere above; `top3_rate` is never cited.
- **Backtest degraded seasons.** DEF is neutralized (all-zero) in every backtest
  season; K is borrowed from the 2026 pool (identity-random against those years);
  2024 RB/WR/TE and all K projections are synthetic (ECR-rank curve). So the
  backtest differentiates **QB timing only** -- it is silent on DEF/K timing and
  on skill-position projection quality. It is a QB-timing sanity check, not a
  full-strategy oracle. The borrowed-2026-kicker distortion is substantial in
  magnitude, not just a technicality: 63/99 borrowed kickers scored zero of
  their assigned 2023 weeks. It's still rank-safe for the QB-timing conclusion
  because the same borrow-and-zero mechanic applies symmetrically across all
  12 seats.
- **DEF projection precision is weak by construction.** Sleeper's season
  projection is the weak part: flat 9.66 pts/wk uplift, no tier buckets. The
  underlying YoY DEF rank correlation (ρ=0.572, r²≈0.33,
  `2026-07-10-dst-semantics.md`) is moderate and partially usable in its own
  right -- Sleeper's projection just doesn't act on it. The DEF-late conclusion
  leans on the farm's opportunity-cost delta + Phase 2's 2025-exact streaming
  analysis, NOT on the YoY correlation being small.
- **Opponent QB bias (Section 1 caveat).** Sim opponents take QB1 at mean round
  2.84 vs the league's 1.83 -- delayed-QB strategies look safer in sim than they
  will in your real draft. Directional (biased 198-draft sample), but it points
  one way: don't over-delay.
- **Slot-vs-human annotation coverage.** Opponent priors blend all humans per
  slot (recency-weighted); only slot 12 is annotated today. A per-manager
  annotation pass would sharpen the opponent model and is the highest-value
  pending user input.
- **Bonus-pricing scope.** The weekly-gamma bonus EV correction
  (`2026-07-10-bonus-ev-valuation-diff.md`) is baked into the valuation these
  farm pools were built from; it shifts QB VORP down ~10-15 pts but reorders the
  top-25 only mildly, so it does not change any conclusion here.

---

<!-- BEGIN GENERATED EVIDENCE (scripts/strategy_conclusions.py) -->

_Generated 2026-07-10T12:47:52 from farm run dated 2026-07-10 (git 652d1b4119ea902d2db9ba1c900a64608ec63489)._

_Data vintage: ADP snapshot #6 (5.15h old at farm time), valuation computed 2026-07-10T09:05:21.468303-07:00, priors latest_season 2025, degraded=False._

### Farm QB-policy (qb_subgrid, defk_round=14) -- all-play% by qb_plan x scenario

_Cross-cell deltas only; absolute levels are MC-inflated (see doc caveats)._

| qb_plan | qb_not_before | qb_by_round | scenario | all-play% | +/- 1.96se |
|---|---|---|---|---|---|
| 0 | (1, 1, 1) | (1, 4, 9) | qb_hoard_0 | 70.4% | +/- 0.4% |
| 0 | (1, 1, 1) | (1, 4, 9) | qb_hoard_12 | 69.1% | +/- 0.4% |
| 0 | (1, 1, 1) | (1, 4, 9) | qb_hoard_24 | 68.6% | +/- 0.5% |
| 1 | (1, 3, 6) | (2, 5, 9) | qb_hoard_0 | 71.5% | +/- 0.4% |
| 1 | (1, 3, 6) | (2, 5, 9) | qb_hoard_12 | 70.0% | +/- 0.5% |
| 1 | (1, 3, 6) | (2, 5, 9) | qb_hoard_24 | 70.6% | +/- 0.4% |
| 2 | (2, 5, 9) | (3, 6, 10) | qb_hoard_0 | 71.8% | +/- 0.4% |
| 2 | (2, 5, 9) | (3, 6, 10) | qb_hoard_12 | 71.3% | +/- 0.4% |
| 2 | (2, 5, 9) | (3, 6, 10) | qb_hoard_24 | 71.4% | +/- 0.4% |
| 3 | (3, 6, 10) | (4, 8, 12) | qb_hoard_0 | 71.9% | +/- 0.4% |
| 3 | (3, 6, 10) | (4, 8, 12) | qb_hoard_12 | 71.6% | +/- 0.4% |
| 3 | (3, 6, 10) | (4, 8, 12) | qb_hoard_24 | 71.9% | +/- 0.4% |
| 4 | (1, 2, 4) | (2, 4, 6) | qb_hoard_0 | 70.7% | +/- 0.4% |
| 4 | (1, 2, 4) | (2, 4, 6) | qb_hoard_12 | 68.8% | +/- 0.5% |
| 4 | (1, 2, 4) | (2, 4, 6) | qb_hoard_24 | 69.2% | +/- 0.5% |
| 5 | (1, 4, 99) | (2, 7, 19) | qb_hoard_0 | 71.2% | +/- 0.4% |
| 5 | (1, 4, 99) | (2, 7, 19) | qb_hoard_12 | 70.4% | +/- 0.5% |
| 5 | (1, 4, 99) | (2, 7, 19) | qb_hoard_24 | 70.2% | +/- 0.4% |

### Farm DEF/K policy (main grid, scenario qb_hoard_12) -- all-play% by defk_round

_defk_round = round at which DEF is force-drafted (K at defk_round+1) if still unheld._

| defk_round | mean all-play% | n cells | delta vs earliest |
|---|---|---|---|
| 8 | 66.3% | 12 | +0.0% |
| 11 | 68.0% | 12 | +1.7% |
| 14 | 70.1% | 12 | +3.8% |
| 18 | 71.1% | 12 | +4.8% |

### Farm tier-break delta (main grid)

| tier_break_bonus | mean all-play% | n cells |
|---|---|---|
| 0.0 | 69.0% | 24 |
| 8.0 | 68.8% | 24 |

Delta (tier_break=8.0 minus tier_break=0.0): -0.1%

### R7 sim-vs-backtest agreement (QB timing, defk_round=18)

Spearman rho over the 6 common QB plans: **0.200** (p=0.704, n=6 -- low power; read the rank agreement, not the p-value).

Farm side = today's main-grid all-play% at defk_round=18, tier_break=0.0 (matched to the backtest default). Backtest side = the SAME 6 (qb_not_before, qb_by_round) plans re-run in memory through ffi.sim.backtest: 3 seasons x 100 seeded drafts, scored on ACTUAL nflverse points, defk_round=18, DEF neutralized. NOTHING persisted; the ADR D7 reference is untouched.

| qb_plan | qb_not_before | farm all-play% | farm rank* | backtest composite | backtest rank* | bt per-season (23/24/25) | bt QB1-round |
|---|---|---|---|---|---|---|---|
| 0 | (1, 1, 1) | 69.7% | 1 | 52.4% | 1 | 49/55/52 | 1/1/1 |
| 1 | (1, 3, 6) | 71.2% | 4 | 56.1% | 5 | 49/61/58 | 1/1/1 |
| 2 | (2, 5, 9) | 72.7% | 5 | 55.5% | 4 | 51/52/63 | 2/2/2 |
| 3 | (3, 6, 10) | 72.8% | 6 | 55.0% | 2 | 46/53/66 | 3/3/3 |
| 4 | (1, 2, 4) | 70.0% | 2 | 55.1% | 3 | 52/57/56 | 1/1/1 |
| 5 | (1, 4, 99) | 70.4% | 3 | 58.0% | 6 | 54/58/62 | 1/1/1 |

_*rank 1 = worst all-play% within its own method (6 = best). Both methods independently rank qb_plan 0 (front-load QB, qb_not_before=(1,1,1)) WORST; beyond that the fine ordering diverges -- see the hand-written adjudication above._

<!-- END GENERATED EVIDENCE -->
